import React from "react";
import { Outlet } from "react-router-dom";

const RestaurantMainLyout = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default RestaurantMainLyout;
